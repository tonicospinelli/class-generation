language: php

php:
  - 5.4
  - 5.5

before_script:
  - "composer self-update"
  - "composer --version"
  - "composer install --prefer-dist --verbose"
  - "composer install --dev"

script:
  - "./vendor/bin/phpunit"
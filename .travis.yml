language: php

php:
  - 5.4
  - 5.5

before_script:
  - "composer self-update"
  - "composer --version"
  - "composer install --dev --prefer-source"
  - "bin/composer install --dev --prefer-source"

script:
  - "./vendor/bin/phpunit"